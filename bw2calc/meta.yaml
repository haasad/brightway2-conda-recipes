package:
  name: bw2calc
  version: "1.5.4"

source:
  fn: bw2calc-1.5.4.tar.gz
  url: https://pypi.python.org/packages/a1/ba/0205651be8777c1afe6a56ed4ee4a9df122c27968e9802cc02655e44deda/bw2calc-1.5.4.tar.gz
  md5: 1d91f4bc279b3589f4ed89a758f79be5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch: python
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bw2calc = bw2calc:main
    #
    # Would create an entry point called bw2calc that calls bw2calc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eight
    - numpy
    - scipy
    - stats_arrays

  run:
    - python
    - eight
    - numpy
    - scipy
    - stats_arrays

test:
  # Python imports
  imports:
    - bw2calc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cmutel/brightway2-calc
  license: BSD License
  summary: ''
  license_family: BSD

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
